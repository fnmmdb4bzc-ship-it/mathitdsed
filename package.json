{
  "name": "mathit",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MathIT: static practice app plus one Netlify function for the API.",
  "scripts": {
    "build":   "node scripts/build-site.mjs",
    "migrate": "node scripts/migrate.mjs",
    "dev":     "netlify dev"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.10.4",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "pg": "^8.13.1"
  },
  "engines": {
    "node": ">=20"
  }
}
